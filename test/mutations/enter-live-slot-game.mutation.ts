export const enterLiveSlotsGameMutation = (machineId: string): string => `
mutation EnterLiveSlotsGame {
    enterLiveSlotsGame(input: {machineId: "${machineId}", version: V2}) {
        gameUrl
        iframeData {
            api
            balance
            gamestream
            gender
            hand
            header
            id
            imei
            lang
            machineId
            muteSfx
            muteStream
            password
            playerId
            reconnect
            soundEffect
            token
            username
            gameData {
                streamMain
                streamMain2
                streamMainWidth
                streamMainHeight
                streamMainId
                streamMainEndpoint
                streamMainEndpoint2
                streamTop
                streamTop2
                streamTopWidth
                streamTopHeight
                streamTopId
                streamTopEndpoint
                streamTopEndpoint2
                streamCam
                streamCam2
                streamCamWidth
                streamCamHeight
                streamCamId
                streamCamEndpoint
                streamCamEndpoint2
                spinTime
                spinRound
                clscIp
                clscPort
                streamCamAspectRatio
                machinename
                sessionId
                gameName
                manufacturerId
                manufacturerName
                modelName
                modelId
            }
        }
    }
}
`;
